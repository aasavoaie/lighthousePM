class AnalyticsService:
    """Deterministic metrics computation placeholder."""

    def recompute_release_metrics(self, release_id: str) -> None:
        raise NotImplementedError("Metrics computation is not implemented in this scaffold.")
